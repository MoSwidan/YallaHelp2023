function All() {
    window.addEventListener("scroll", () => {
        let nav = document.querySelector(".my-nav");
        //if (window.scrollY > 0 && nav.classList.contains("position-fixed") != true) {
        //    nav.classList.add("bg-white", "position-fixed");
        //}
        //else if (window.scrollY < 10 && nav.classList.contains("position-fixed") == true) {
        //    nav.classList.remove("bg-white", "position-fixed");
        //}
    });
}